{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tw": "tailwind -i resources/css/input.css -o public/build/output.css"
  },

  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
